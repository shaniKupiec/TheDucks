import styled from 'styled-components';

export const StyledGridContainer = styled.div`
    width: 90vw;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 50px;
`

export const StyledTitle = styled.h1`
color: rgba(237, 190, 0, 0.87);
font-size: 60px;
`